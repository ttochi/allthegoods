<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width,initial-scale=1.0,minimum-scale=1.0,maximum-scale=1.0"/>

    <link rel="apple-touch-icon" sizes="57x57" href="/assets/favicon/57x57.png"/>
    <link rel="apple-touch-icon" sizes="72x72" href="/assets/favicon/72x72.png"/>
    <link rel="apple-touch-icon" sizes="114x114" href="/assets/favicon/114x114.png"/>
    <link rel="apple-touch-icon" sizes="144x144" href="/assets/favicon/144x144.png"/>
    <link rel="shortcut icon" href="/assets/favicon/144x144.png"/>

    <title>FANMADE-굿즈모아봤다</title>

    <script>
        (function (i, s, o, g, r, a, m) {
            i['GoogleAnalyticsObject'] = r;
            i[r] = i[r] || function () {
                      (i[r].q = i[r].q || []).push(arguments)
                  }, i[r].l = 1 * new Date();
            a = s.createElement(o),
                  m = s.getElementsByTagName(o)[0];
            a.async = 1;
            a.src = g;
            m.parentNode.insertBefore(a, m)
        })(window, document, 'script', 'https://www.google-analytics.com/analytics.js', 'ga');

        ga('create', 'UA-85274557-1', 'auto');
        ga('send', 'pageview');
    </script>

    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
    <%= yield(:js_css) %>
    <%= csrf_meta_tags %>
</head>
<body>

<div class="nav">
    <%= link_to '', root_path, class: 'logo' %>
    <% if user_signed_in? %>
        <%= link_to '로그아웃', destroy_admin_session_path, method: :delete, class: 'admin right' %>
    <% elsif admin_signed_in? %>
        <%= link_to '어드민', admins_index_path, class: 'admin left' %>
        <%= link_to '로그아웃', destroy_admin_session_path, method: :delete, class: 'admin right' %>
    <% else %>
        <%= link_to '로그인', new_user_session_path, class: 'admin right' %>
    <% end %>
</div>

<div id="body">
    <%= yield %>
</div>

</body>
</html>